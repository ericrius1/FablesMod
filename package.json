{
  "name": "ericsmod",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tweakpane/core": "^2.0.5",
    "box3d-wasm": "file:../box3d-wasm/packages/box3d-wasm",
    "three": "^0.185.1",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
